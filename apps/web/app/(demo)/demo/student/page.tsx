import { StudentClient } from '@/app/(standalone)/evaluation-models/[id]/test/result/student/student-client';

export const metadata = { title: '生徒用ビュー（デモ）| EvalEngine' };

export default function DemoStudentPage() {
  return <StudentClient />;
}
