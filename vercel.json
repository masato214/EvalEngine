{
  "buildCommand": "pnpm turbo run build --filter=@evalengine/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --prod=false",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/v1/analysis/sweep",
      "schedule": "0 18 * * *"
    }
  ]
}
